# ── Web framework ────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0        # ASGI server with websocket + reload support

# ── Database ─────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0       # async ORM
aiosqlite>=0.19.0                # async SQLite driver (dev)
# asyncpg>=0.29.0                # async Postgres driver (prod) — uncomment when ready

# ── Validation & serialisation ───────────────────────────
pydantic>=2.0.0
python-multipart>=0.0.9          # required for FastAPI form data

# ── HTTP client (for testing / future webhooks) ──────────
httpx>=0.27.0

# ── LLM (extracurricular assessment) ─────────────────────
anthropic>=0.40.0
