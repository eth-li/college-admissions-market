# Runtime dependencies for the FastAPI backend.
# Railway installs this automatically on deploy.
# asyncpg is used in production (Postgres); aiosqlite is used locally (SQLite).

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.19.0
asyncpg>=0.29.0
pydantic>=2.0.0
python-multipart>=0.0.9
httpx>=0.27.0
anthropic>=0.40.0
